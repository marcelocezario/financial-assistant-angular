import { AppRelativeTimePipe } from './app-relative-time.pipe';

describe('AppRelativeTimePipe', () => {
  it('create an instance', () => {
    const pipe = new AppRelativeTimePipe();
    expect(pipe).toBeTruthy();
  });
});
