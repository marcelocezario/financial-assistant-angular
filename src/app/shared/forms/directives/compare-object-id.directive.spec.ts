import { CompareObjectIdDirective } from './compare-object-id.directive';

describe('CompareObjectIdDirective', () => {
  it('should create an instance', () => {
    const directive = new CompareObjectIdDirective();
    expect(directive).toBeTruthy();
  });
});
